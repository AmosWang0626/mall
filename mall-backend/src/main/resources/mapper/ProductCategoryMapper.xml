<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.module.product.mapper.ProductCategoryMapper">

    <resultMap id="BaseResultMap" type="com.mall.module.product.entity.ProductCategory">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="name" property="name"/>
        <result column="icon" property="icon"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="level" property="level"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, parent_id, name, icon, sort, status, level, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="com.mall.module.product.entity.ProductCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_category (parent_id, name, icon, sort, status, level)
        VALUES (#{parentId}, #{name}, #{icon}, #{sort}, #{status}, #{level})
    </insert>

    <update id="updateById" parameterType="com.mall.module.product.entity.ProductCategory">
        UPDATE product_category
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="level != null">level = #{level},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE product_category SET deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product_category WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product_category WHERE deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort ASC, id ASC
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product_category
        WHERE parent_id = #{parentId} AND deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort ASC, id ASC
    </select>

    <select id="selectMaxSort" resultType="int">
        SELECT MAX(sort) FROM product_category WHERE parent_id = #{parentId} AND deleted = 0
    </select>
</mapper>
