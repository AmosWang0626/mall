<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.module.product.mapper.ProductSkuMapper">

    <resultMap id="BaseResultMap" type="com.mall.module.product.entity.ProductSku">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="sku_code" property="skuCode"/>
        <result column="name" property="name"/>
        <result column="specs" property="specs"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
        <result column="image" property="image"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, product_id, sku_code, name, specs, price, stock, image, status,
        create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="com.mall.module.product.entity.ProductSku" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_sku (product_id, sku_code, name, specs, price, stock, image, status)
        VALUES (#{productId}, #{skuCode}, #{name}, #{specs}, #{price}, #{stock}, #{image}, #{status})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO product_sku (product_id, sku_code, name, specs, price, stock, image, status)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.productId}, #{item.skuCode}, #{item.name}, #{item.specs},
             #{item.price}, #{item.stock}, #{item.image}, #{item.status})
        </foreach>
    </insert>

    <update id="updateById" parameterType="com.mall.module.product.entity.ProductSku">
        UPDATE product_sku
        <set>
            <if test="productId != null">product_id = #{productId},</if>
            <if test="skuCode != null">sku_code = #{skuCode},</if>
            <if test="name != null">name = #{name},</if>
            <if test="specs != null">specs = #{specs},</if>
            <if test="price != null">price = #{price},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="image != null">image = #{image},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE product_sku SET deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product_sku WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product_sku
        WHERE product_id = #{productId} AND deleted = 0
        ORDER BY id ASC
    </select>

    <update id="deleteByProductId">
        UPDATE product_sku SET deleted = 1 WHERE product_id = #{productId}
    </update>

    <update id="reduceStock">
        UPDATE product_sku
        SET stock = stock - #{quantity}
        WHERE id = #{id} AND deleted = 0 AND stock &gt;= #{quantity}
    </update>

    <update id="restoreStock">
        UPDATE product_sku
        SET stock = stock + #{quantity}
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="sumStockByProductId" resultType="int">
        SELECT IFNULL(SUM(stock), 0) FROM product_sku
        WHERE product_id = #{productId} AND deleted = 0
    </select>
</mapper>
